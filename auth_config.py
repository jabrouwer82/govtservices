import config
# To do authenticate on your local machine, edit the following lines to point
# to your personal credentials.
auth = {
    'username': config.DEFAULT_USERNAME,
    'password': config.DEFAULT_PASSWORD
}
